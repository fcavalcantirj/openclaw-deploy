[
  {"category": "scripts", "description": "Create provision.sh that creates Hetzner VM with SSH access", "steps": ["Accept --name and --region flags", "Generate SSH key if not exists", "Upload SSH key to Hetzner", "Create cx22 Ubuntu 24.04 server", "Wait for boot, verify SSH works", "Output server IP"], "passes": true},
  {"category": "scripts", "description": "Create bootstrap.sh that prepares VM for Claude Code", "steps": ["Run via SSH on fresh VM", "apt update && upgrade", "Install essentials: curl git jq ufw tmux", "Install Node 22 via nodesource", "Install Claude Code CLI globally", "Configure UFW: allow SSH only", "Create openclaw user with sudo", "Output success message"], "passes": true},
  {"category": "scripts", "description": "Create setup-openclaw.sh that runs Claude Code on VM to configure everything", "steps": ["Accept ANTHROPIC_API_KEY as argument or env", "SSH to VM as openclaw user", "Set ANTHROPIC_API_KEY on VM", "Run claude with setup prompt to install OpenClaw", "Claude Code handles: npm install openclaw, gateway config, systemd service", "Output Claude Code result"], "passes": false},
  {"category": "prompts", "description": "Create prompts/setup-openclaw.md for Claude Code on VM", "steps": ["Phase 1: npm install -g openclaw, verify version", "Phase 2: openclaw gateway install, enable systemd, configure loopback+token auth", "Phase 3: Configure telegram channel (placeholder token, dmPolicy pairing)", "Phase 4: Enable chatCompletions HTTP endpoint", "Verify: openclaw gateway status shows running"], "passes": false},
  {"category": "scripts", "description": "Create setup-tailscale.sh that runs Claude Code on VM for Tailscale", "steps": ["SSH to VM", "Run claude with tailscale prompt", "Install tailscale, run tailscale up", "Capture and output auth URL for user", "Wait for user to authenticate (poll status)", "Output tailscale IP when connected"], "passes": false},
  {"category": "scripts", "description": "Create setup-skills.sh that configures OpenAI Whisper and ClawdHub", "steps": ["Accept OPENAI_API_KEY as argument", "SSH to VM", "Configure openai-whisper-api skill with API key", "Install clawhub CLI globally", "Verify whisper transcribe.sh works", "Verify clawhub --version works"], "passes": false},
  {"category": "scripts", "description": "Create setup-monitoring.sh that installs healthcheck on VM", "steps": ["SSH to VM", "Create ~/scripts/healthcheck.sh", "Create systemd user timer (5 min interval)", "Enable timer", "Create logrotate config", "Verify timer is active"], "passes": false},
  {"category": "scripts", "description": "Create verify.sh that runs full verification checklist", "steps": ["SSH to VM and check: openclaw version, gateway status, systemd service, tailscale status, UFW status, disk space, memory", "Output pass/fail for each check", "Generate QUICKREF.md on VM", "Output summary"], "passes": false},
  {"category": "scripts", "description": "Create deploy.sh master script that orchestrates everything", "steps": ["Accept --name, --region, --anthropic-key, --openai-key flags", "Call provision.sh", "Call bootstrap.sh", "Call setup-openclaw.sh", "Call setup-tailscale.sh (pause for user auth)", "Call setup-skills.sh", "Call setup-monitoring.sh", "Call verify.sh", "Output final summary with all access details"], "passes": false},
  {"category": "scripts", "description": "Create destroy.sh for clean teardown", "steps": ["Accept instance name", "Require --confirm flag", "Tailscale logout on VM", "Delete Hetzner server", "Delete SSH keys", "Archive metadata", "Output confirmation"], "passes": false},
  {"category": "scripts", "description": "Create status.sh and list.sh for instance management", "steps": ["status.sh: show instance details from metadata + live checks via SSH", "list.sh: list all instances with status summary", "Both scripts handle offline/unreachable VMs gracefully"], "passes": false},
  {"category": "templates", "description": "Create templates/openclaw.json base config", "steps": ["gateway: bind loopback, port 18789, auth token", "telegram: enabled, dmPolicy pairing", "logging: redactSensitive true", "http: chatCompletions enabled"], "passes": false},
  {"category": "documentation", "description": "Create comprehensive README with usage examples", "steps": ["Prerequisites section", "Quick start: ./deploy.sh --name mybot", "Manual step-by-step instructions", "Troubleshooting section", "Cost breakdown"], "passes": false},
  {"category": "documentation", "description": "Update CLAUDE.md with correct workflow", "steps": ["Golden rules for script writing", "Testing approach: scripts should be idempotent", "No testing by actually deploying VMs", "Verify scripts work via code review"], "passes": false}
]
